# --- CONFIGURACIÓN DE APLICACIÓN ---
APP_NAME=Monitor
APP_ENV=local
APP_KEY=base64:E36cj5H2/fuUlvR2UYCvNC03s86kCKPA5wVqbiQkFFs=
APP_DEBUG=false
APP_TIMEZONE=America/Lima
APP_URL=http://127.0.0.1:8000
ASSET_URL=http://127.0.0.1:8000
APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_PE

# --- CONFIGURACIÓN DE BASE DE DATOS ---
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=satindex_monitor
DB_USERNAME=root
DB_PASSWORD=

# --- BROADCASTING (SOLO REVERB) ---

# Se eliminó la duplicidad de BROADCAST_CONNECTION=log
BROADCAST_CONNECTION=reverb

# --- REVERB (SERVIDOR PHP) ---
REVERB_APP_ID=my-app-id
REVERB_APP_KEY=my-app-key
REVERB_APP_SECRET=my-app-secret
REVERB_HOST=127.0.0.1
REVERB_PORT=8080
REVERB_SCHEME=http

# --- REVERB (FRONTEND / VITE) ---
VITE_REVERB_APP_KEY=my-app-key 
VITE_REVERB_HOST=
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https

# --- SESIÓN & CACHÉ ---

# Configurado para usar base de datos para mayor estabilidad en producción
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=
SESSION_SECURE_COOKIE=false

# --- CONFIGURACIÓN DE REDIS ---
REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=wehc8pf0pqglzv6r
REDIS_PORT=6379
CACHE_STORE=file
CACHE_PREFIX=monitor_cache
QUEUE_CONNECTION=sync

# --- ALMACENAMIENTO (FILESYSTEM) ---
FILESYSTEM_DISK=public
FILAMENT_FILESYSTEM_DISK=public

# --- LOGS ---
LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=debug

# --- CONFIGURACIÓN DE SERVIDOR (NIXPACKS/PHP) ---
LIVEWIRE_MAX_UPLOAD_SIZE=102400
NIXPACKS_PHP_INI=upload_max_filesize=100M;post_max_size=100M;memory_limit=512M
NIXPACKS_NGINX_CLIENT_MAX_BODY_SIZE=100M

# --- CORREO ---
MAIL_MAILER=log
MAIL_FROM_ADDRESS="admin@sat-sistemas.uk"
MAIL_FROM_NAME="${APP_NAME}"

# --- NOTIFICACIONES PUSH (VAPID) ---
VAPID_SUBJECT=mailto:admin@sat-sistemas.uk
VAPID_PUBLIC_KEY=BEjh6uQ0KyvJTDsdXW03vZZAMyUcpjI40HHZSdMYgP4jlWToQzEeOvtvvnP-KfZNWUrEa9XkUqnR-tgSjMoP-vw
VAPID_PRIVATE_KEY=AvO9HsFQ3-0oIhECLvRL_gRadvvzEw32cBsiHOQExDI=